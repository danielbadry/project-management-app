@page "/register"
@using AppHost.Web.Components.Layouts
@layout AuthLayout

@rendermode @(new InteractiveServerRenderMode(prerender: false))

<PageTitle>Register</PageTitle>

<section class="login-shell" aria-labelledby="login-title">
    <div class="login-panel">
        <div class="login-heading">
            <span class="login-badge">Project Management</span>
            <h1 id="login-title">Register</h1>
        </div>

        <EditForm Model="@RegisterDto" FormName="Register" OnValidSubmit="HandleRegister" class="login-form">
            <DataAnnotationsValidator />
            <div class="field">
                <label for="name">Name</label>
                <input id="name" class="field-input" autocomplete="name" @bind="RegisterDto.Name" />
                <ValidationMessage For="@(() => RegisterDto.Name)" />
            </div>
            <div class="field">
                <label for="email">Email</label>
                <input id="email" type="email" class="field-input" autocomplete="email" @bind="RegisterDto.Email" />
                <ValidationMessage For="@(() => RegisterDto.Email)" />
            </div>
            <div class="field">
                <label for="username">Username</label>
                <input id="username" class="field-input" autocomplete="username" @bind="RegisterDto.Username" />
                <ValidationMessage For="@(() => RegisterDto.Username)" />
            </div>

            <div class="field">
                <label for="password">Password</label>
                <input id="password" name="password" type="password" class="field-input" autocomplete="new-password"
                       @bind="RegisterDto.Password" @bind:event="oninput" />
                <ValidationMessage For="@(() => RegisterDto.Password)" />
            </div>
            <div class="field">
                <label for="confirm-password">Confirm password</label>
                <input id="confirm-password" name="confirmPassword" type="password" class="field-input"
                       autocomplete="new-password" @bind="RegisterDto.ConfirmPassword" @bind:event="oninput" />
                <ValidationMessage For="@(() => RegisterDto.ConfirmPassword)" />
            </div>

            @if (!string.IsNullOrWhiteSpace(RegisterErrorMessage))
            {
                <div class="form-error" role="alert">@RegisterErrorMessage</div>
            }

            <button class="login-button" type="submit">Register</button>
        </EditForm>
    </div>
</section>
